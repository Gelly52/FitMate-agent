# 生活娱乐赛道 | FitMate-AI：你的 AI 健身私教

> 一个基于自主 ReAct Agent 架构的全栈 AI 健身助手——它能思考、能调用工具、能记住你、还能派出"分身"处理复杂任务。

------

## 一、Demo 简介

**是什么：** FitMate-AI 是一个全栈 Web 应用（Spring Boot 3 + Vue 3 + TypeScript），核心是一个自主 ReAct（Reason + Act）Agent，能够像真正的私教一样：理解你的需求 → 查询你的数据 → 分析训练状态 → 给出个性化建议 → 记录到长期记忆。

**面向谁：** 有健身习惯的运动爱好者——从入门到进阶，需要一个能持续跟踪训练数据、理解身体变化、并给出科学建议的智能助手。

**核心功能：**

**1. 自主 Agent 推理与工具调用**
Agent 不是简单的"问→答"，而是进入一个完整的 ReAct 循环：推理（thinking）→ 决策（JSON action）→ 执行（tool call）→ 观察（observation）→ 继续推理……最多 20 轮迭代、100 次工具调用。用户可以在前端实时看到 Agent 的每一步思考和操作。

**2. 21 个内置工具 + 动态 MCP 扩展**
Agent 拥有覆盖健身数据全生命周期的工具链：力量训练记录/查询/修改、有氧运动记录（自动计算配速）、心率记录、饮食记录（自动计算热量与宏量营养素）、身体指标管理、Wiki 知识库搜索、RAG 文档检索、SearXNG 联网搜索、网页抓取、长期记忆读写、技能按需加载。同时支持每用户独立配置外部 MCP 服务器，动态发现并桥接外部工具。

**3. 实时流式推理展示 + 完整执行追踪**
DeepSeek 的 reasoning_content（思维链）和最终回答通过 SSE 实时流式推送到前端，用户能看着 Agent 一步步思考。每一步推理、工具调用、子 Agent 派生都持久化到数据库，支持历史回放。

![Agent 推理过程实时展示](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%20Agent%20%E6%8E%A8%E7%90%86%E7%95%8C%E9%9D%A2%E7%9A%84%E6%88%AA%E5%9B%BE)

------

## 二、Demo 创作思路

**灵感来源：** 我自己一直在健身，也一直在记录训练数据。但市面上的健身 App 有两个痛点：第一，数据记录和数据分析是割裂的——记完数据还要自己看图表、自己分析；第二，"AI 健身助手"大多是套壳聊天机器人，问它问题它只会泛泛而谈，根本不知道"我"是谁、我上周练了什么、我的体脂趋势怎样。我想要一个真正"了解我"的 Agent。

**想解决的问题：**

- 痛点一：数据孤岛。训练数据、身体指标、饮食记录散落在不同功能里，用户要自己整合分析。FitMate-AI 的 Agent 可以自主决定调用哪些工具、跨数据源关联分析。
- 痛点二：AI 不懂你。传统聊天机器人没有记忆，每次对话从零开始。FitMate-AI 有四层记忆体系（短期上下文 → 上下文压缩 → 长期记忆 → 用户画像），Agent 知道你的训练习惯、身体数据趋势、甚至之前的关键事件。
- 痛点三：扩展性差。传统 AI 应用的能力是写死的。FitMate-AI 通过 MCP 协议让每个用户接入自己的外部工具，Agent 能力可以无限扩展。

**为什么做这个方向：** 健身是一个数据密集 + 知识密集的领域，既有结构化数据（训练组数、重量、心率、体重）适合工具调用，又有非结构化知识（训练计划、恢复策略、营养学）适合 LLM 推理。这个场景天然适合 ReAct Agent 架构——能把"查数据"和"给建议"统一步骤化，而不是让用户在多个页面间跳转。

------

## 三、Demo 体验地址

<!-- 如果有公网部署的体验链接，请替换下方链接 -->
体验地址：`此处替换为公网可访问的体验链接`

<!-- 如果暂时无法公网部署，可将前端打包为 HTML 并 Zip 上传社区 -->
<!-- 备选方案：交互式 HTML 文件打包上传 -->

------

## 四、TRAE 实践过程

> 以下展示用 TRAE 完成 Demo 开发的完整流程，包含关键步骤截图与 Session ID。

### 技术架构总览

| 层级      | 技术选型                                                     |
| --------- | ------------------------------------------------------------ |
| 前端      | Vue 3 + TypeScript + Vite + Tailwind CSS (Material Design 3) |
| 后端      | Java 21 + Spring Boot 3.5 + Spring AI 1.1 + MyBatis-Plus     |
| 数据库    | MySQL 8                                                      |
| 缓存/向量 | Redis Stack（含向量存储）                                    |
| 搜索引擎  | SearXNG（自托管元搜索引擎）                                  |
| LLM       | DeepSeek V4（OpenAI 兼容 API，支持思维链）                   |
| Embedding | BGE-M3（1024 维）                                            |
| MCP       | 自研 MCP Server 模块 + 每用户动态 MCP Client                 |
| 基础设施  | Docker Compose（MySQL + Redis Stack + SearXNG）              |

后端采用 Maven 多模块架构：`FitMate-api`（主业务，端口 7070）、`FitMate-mcpServer`（MCP 工具服务，端口 9070）、`FitMate-common`（共享组件）。

### 开发流程

#### 第一步：项目初始化与基础架构搭建

用 TRAE 创建项目骨架，定义 Spring Boot 多模块结构、Vue 3 前端工程、Docker Compose 基础设施编排。通过 TRAE 的对话式开发，快速生成了项目目录结构、`pom.xml` 依赖配置、`docker-compose.yml`、以及前后端的 `SPEC.md` 编码规范文档。

![项目架构搭建截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84%E6%88%96%20TRAE%20%E5%AF%B9%E8%AF%9D%E6%88%AA%E5%9B%BE)
Session ID：`此处替换为 TRAE 对话 Session ID`

#### 第二步：Agent 核心引擎——ReAct 循环

这是整个项目的核心。通过 TRAE 对话，逐步构建了 `AgentLoopExecutor`（959 行），实现了完整的 ReAct 循环：

- **推理阶段**：调用 DeepSeek 流式 API，实时解析 `reasoning_content`（思维链）和 `content`（决策 JSON）
- **决策解析**：LLM 输出三种 JSON action：`tool_call`（调用工具）、`spawn_subagent`（派生子 Agent）、`final`（给出最终回答）
- **工具执行**：通过 `ToolRouter` 路由到内置工具或用户 MCP 工具，记录输入输出
- **观察反馈**：工具结果作为 observation 注入下一轮决策 prompt
- **预算控制**：最多 20 轮迭代、100 次工具调用、30 分钟超时

关键创新点是 `FinalAnswerStreamState`——一个 4 状态有限状态机，在 LLM 流式输出 JSON 的过程中实时解析 `final_answer` 字段值，让用户在 LLM 还在生成 JSON 包裹的同时就能看到回答逐字出现。

![Agent 核心引擎开发截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%20AgentLoopExecutor%20%E4%BB%A3%E7%A0%81%E6%88%96%20TRAE%20%E5%AF%B9%E8%AF%9D%E6%88%AA%E5%9B%BE)
Session ID：`此处替换为 TRAE 对话 Session ID`

#### 第三步：SSE 流式推送优化

用 TRAE 设计了 SSE 流式架构，解决了高频 SSE 推送导致的锁竞争问题：

- `SseChunkBuffer`：50ms 时间窗口或 4096 字符阈值的批量缓冲器，减少 SSE 调用频率
- `SSEServer`：线程安全的 `ConcurrentHashMap<sseClientId, SseEmitter>` 连接管理，支持重连和竞态安全的移除
- 思维链（THINKING）和回答内容（ADD）分通道推送，保证前端渲染顺序
- `subscribeOn(Schedulers.boundedElastic)` 让流式消费与生产并行，而非等待整个 LLM 响应完成

![SSE 流式架构截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%20SSE%20%E6%9E%B6%E6%9E%84%E6%88%96%E5%89%8D%E7%AB%AF%E6%B5%81%E5%BC%8F%E6%95%88%E6%9E%9C%E6%88%AA%E5%9B%BE)

#### 第四步：Sub-Agent 子智能体编排

Agent 遇到复杂任务时可以自主决定派生子 Agent。通过 TRAE 对话完成了完整的子 Agent 架构设计（设计文档 644 行）：

- 主 Agent 输出 `{"action":"spawn_subagent","task":"...","allowed_tools":"..."}` 触发
- 创建子 run（`parent_run_id` 关联），独立预算（8 轮迭代、20 次工具调用、10 分钟）
- 递归调用同一个 `runLoop()` 方法（但禁止子 Agent 再派生，防止无限递归）
- 结果作为 observation 回传主 Agent
- 级联取消：取消主 Agent 时自动取消活跃子 Agent
- 前端渲染为嵌套可折叠的追踪块，带脉冲动画

![Sub-Agent 嵌套追踪截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%E5%AD%90%20Agent%20%E5%89%8D%E7%AB%AF%E5%B1%95%E7%A4%BA%E6%88%AA%E5%9B%BE)

#### 第五步：多层级记忆系统

通过 TRAE 构建了四层记忆体系：

- **短期记忆**：加载最近 50 条对话作为 prompt 上下文
- **上下文压缩**：token 用量超过 80% 时自动触发 LLM 摘要，老消息压缩为 summary，支持手动和被动两种触发模式
- **长期记忆**：四种类型——FACT（稳定事实）、EPISODIC（关键事件）、INSIGHT（分析结论）、SNAPSHOT（周期快照）。SHA-256 去重，持久化到 `t_user_memory`
- **用户画像**：LLM 生成的用户档案，24 小时缓存，注入每次决策 prompt
- **每日快照**：每天凌晨 2 点自动聚合过去 14 天的训练 + 身体数据为自然语言 SNAPSHOT 记忆
- **会话记忆提取**：每 5 轮对话后异步提取记忆，复用 Agent prompt 前缀以命中 KV Cache

#### 第六步：工具系统与 MCP 动态扩展

通过 TRAE 实现了 21 个内置工具 + 每用户 MCP 动态发现：

内置工具覆盖：力量训练（记录/查询/修改）、有氧运动（自动计算配速）、心率记录、饮食记录（自动计算热量与宏量营养素）、身体指标管理、Wiki 知识库搜索、RAG 文档检索、SearXNG 联网搜索、网页内容抓取、长期记忆读写、技能按需加载。

所有工具实现统一的 `ToolExecutor` 接口（`descriptor()` + `execute()`），通过 Spring DI 自动注册到 `ToolRegistry`。`ToolRouter` 先查本地工具（受 `enabled-tools` 白名单控制），再查用户专属 MCP 工具。

MCP 层面：每用户独立的 `McpClientPool`（`HttpClientSseClientTransport`），TCP 端口预探测（3 秒超时），`McpToolRegistry` 按用户懒加载 MCP 工具配置，`McpToolExecutorBridge` 将 MCP 工具适配为本地 `ToolExecutor` 接口——让 Agent 调用外部工具时完全透明无感知。

![工具系统截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%E5%B7%A5%E5%85%B7%E8%B0%83%E7%94%A8%E8%BF%87%E7%A8%8B%E6%88%AA%E5%9B%BE)

#### 第七步：DeepSeek KV Cache 优化

这是一个贯穿整个 Agent 系统的工程优化。通过 TRAE 对话分析 DeepSeek 的 KV Cache 机制后，刻意设计了稳定的 prompt 前缀：

- 系统提示词 + 工具 JSON + 记忆摘要 + 用户画像 = 稳定前缀，在一次 run 内跨迭代保持不变
- `AgentPromptBuilder.buildPromptPrefix()` 方法被 Agent 决策和记忆提取共享
- 用户画像在一次 run 内只计算一次（而非每轮迭代），因为执行期间不会变
- 子 Agent 复用相同的 prompt 前缀
- 这样只有追加的 observation 和当前问题是 cache miss，大幅降低 token 开销

#### 第八步：前端 Agent 追踪可视化

用 TRAE 构建了完整的前端 Agent 推理过程可视化：

- `ReasoningTraceBlock.vue`：主推理时间线，将 steps（来自 SSE / DB）与思维链片段按 `iterationNo` 交错合并为统一时间线
- `SubAgentTraceBlock.vue`：子 Agent 嵌套追踪块，可折叠、带脉冲动画、交错展示 LLM 回合与思维链
- `AgentStepCard.vue`：卡片组件，展示步骤摘要、工具调用状态、进度指标
- `AgentRunsPanel.vue`：全局通知下拉框，轮询展示运行中的 Agent 任务（8 秒间隔）、徽章计数、状态指示灯

![前端推理追踪截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%E5%89%8D%E7%AB%AF%20Agent%20%E8%BF%BD%E8%B8%AA%E7%95%8C%E9%9D%A2%E6%88%AA%E5%9B%BE)

#### 第九步：并发控制与协作取消

- **多槽位并发锁**：每个登录会话最多 3 个并发 Agent 任务，3 个独立 Redis 锁槽，各锁独立 TTL，崩溃任务只过期自己的槽
- **锁续期守护线程**：30 秒间隔、120 秒 TTL，compare-and-expire Lua 脚本安全续期
- **协作取消**：用户可随时 `POST /agent/cancel`，`ConcurrentHashMap<runId, cancelFlag>` 在每个迭代边界和 SSE chunk 检查，级联取消子 Agent，保留已生成内容并显示"已停止"

#### 第十步：LLM Wiki 知识库

通过 TRAE 构建了 LLM 编译的知识库系统：

- Wiki 空间和页面由 LLM 从用户对话中自动生成
- 页面使用 frontmatter 元数据，异步编译任务
- 向量 + 关键词混合检索 + 重排序
- 每次 Agent 运行前预搜索 Wiki，结果注入首轮决策 prompt
- 3 个月 TTL，级联清理 Redis 向量 / 关键词索引

### 设计文档与工程规范

项目沉淀了完整的设计文档体系（`docs/superpowers/`），包括：

- Sub-Agent 编排设计文档（644 行）
- SSE 流式优化设计文档
- 长期记忆系统设计文档
- 并发多 run 设计文档
- 思维链持久化与展示设计文档
- 每用户 LLM 配置设计文档
- Wiki 集成设计文档

同时维护了三份 SPEC.md（全局 / 后端 / 前端），定义了项目概述、技术栈、模块架构、包命名、API 约定、数据库 / 缓存约定、配置规范、Java 代码风格、AI/Agent/Memory/RAG/Wiki/MCP 规则、前端约定等。

![设计文档截图](D:/Applications/Java/A%20-%20Learning/FitMate-AI-0/%E6%AD%A4%E5%A4%84%E6%9B%BF%E6%8D%A2%E4%B8%BA%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3%E6%88%96%20SPEC%20%E6%88%AA%E5%9B%BE)

------

## 五、TRAE 开发心得

### 用 TRAE 做对了什么

**1. 对话式开发处理复杂架构。** FitMate-AI 的 Agent 系统涉及异步执行、SSE 流式、ReAct 循环、子 Agent 递归、多层级记忆——每一块都是复杂逻辑。通过 TRAE 的对话式开发，我可以先描述设计意图，再逐步细化为实现。比如 Sub-Agent 编排，我先让 TRAE 帮我生成设计文档，再基于文档逐步实现。

**2. 上下文感知的代码生成。** TRAE 能感知整个项目结构，生成的代码符合既有的包结构和命名规范。比如新增工具时，TRAE 自动按照 `ToolExecutor` 接口 + `descriptor()` + `execute()` 的模式生成，不需要反复纠正。

**3. 跨文件协同修改。** 一次功能迭代往往涉及后端 Java + 前端 TypeScript + SQL + 配置文件。TRAE 能在一次对话中协同修改多个文件，保持前后端接口一致性。

### 踩过的坑

- **Agent prompt 的 JSON 格式稳定性**：LLM 偶尔会输出格式不规范的 JSON，需要 `LlmJsonSanitizer` 做容错清洗
- **SSE 高频推送的锁竞争**：直接逐 chunk 推送会导致 `SseEmitter` 串行锁竞争，需要批量缓冲
- **DeepSeek KV Cache 的 prompt 前缀稳定性**：初期没有注意 prompt 前缀稳定性，导致 cache miss 率高、token 消耗大。后来刻意重构 prompt 构建逻辑，保证前缀稳定
- **子 Agent 无限递归**：最初设计遗漏了递归防护，后来加了"子 Agent 不能再派生子 Agent"的限制

------

## 六、报名帖链接

<!-- 此处替换为社区报名帖链接 -->
报名帖：`此处替换为已通过的社区报名帖链接`

------

## 附录：关键 Session ID 汇总

| 序号 | 开发阶段                  | Session ID              |
| ---- | ------------------------- | ----------------------- |
| 1    | 项目初始化与架构搭建      | `此处替换为 Session ID` |
| 2    | Agent 核心引擎 ReAct 循环 | `此处替换为 Session ID` |
| 3    | SSE 流式推送优化          | `此处替换为 Session ID` |
| 4    | Sub-Agent 子智能体编排    | `此处替换为 Session ID` |
| 5    | 多层级记忆系统            | `此处替换为 Session ID` |
| 6    | 工具系统与 MCP 动态扩展   | `此处替换为 Session ID` |

> Session ID 获取方式：双击 TRAE 的对话即可复制。